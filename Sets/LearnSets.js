const letters = new Set(["a", "b", "c"]);

// Has Method
console.log(letters.has('a')) // return bool value if the item is persent or not in the Set

// Add Method
letters.add('d')
console.log(letters)

console.log(letters.size)


